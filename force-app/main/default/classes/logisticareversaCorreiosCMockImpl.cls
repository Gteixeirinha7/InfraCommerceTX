//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class logisticareversaCorreiosCMockImpl implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
       
		System.debug(LoggingLevel.INFO, 'serviceLogisticareversaCorreiosCMockImpl.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

		if(request instanceOf logisticaReversaCorreios.acompanharPedido) {
			response.put( 'response_x', new logisticaReversaCorreios.acompanharPedidoResponse());
		}
		else if(request instanceOf logisticaReversaCorreios.sobreWebService) {
			response.put( 'response_x', new logisticaReversaCorreios.sobreWebServiceResponse());
		}
		else if(request instanceOf logisticaReversaCorreios.solicitarPostagemReversa) {
			response.put( 'response_x', new logisticaReversaCorreios.solicitarPostagemReversaResponse());
		}
	}
}